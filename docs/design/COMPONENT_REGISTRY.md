# Component Registry

The app currently keeps UI data and markup in `src/app/page.tsx`. Extract
components only when repetition or complexity makes the single-file surface hard
to maintain.

| Component/Section | File                  | Status |
| ----------------- | --------------------- | ------ |
| Cockpit page      | `src/app/page.tsx`    | Active |
| Root layout       | `src/app/layout.tsx`  | Active |
| Global styles     | `src/app/globals.css` | Active |
