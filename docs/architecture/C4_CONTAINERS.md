# C4 Containers

```txt
Browser UI
  -> Next.js App Router
  -> Static/docs governance layer
  -> GitHub PR and issue workflow
  -> Vercel preview workflow
```

Current implementation is a single Next.js app container. Future containers
should only be introduced with an ADR.
